# v0.4 검증 기록

- 전체 C# 소스: Unity 참조 어셈블리를 사용하는 Roslyn 컴파일 통과. 기존 미사용 notice 필드 경고 1개.
- 시뮬레이션 독립 검사: 66/66 통과. 이전 51개 + 신규 15개. Unity 저장 검사 3개는 실제 Editor 실행 대기.
- 던전 기존 정지 사례: 같은 필드의 적을 대상으로 전진하는 회귀 검사 통과.
- 영웅 기존 정지 건물 배치: 15초 후 전진 회귀 검사 통과. 별도 60개 배치 진단에서도 정지 미검출.
- PowerShell 빌드 도우미: 구문 검사 통과.
- Unity Windows/Android v0.4 빌드 및 실제 렌더링: 사용자 계정 실행 대기. 아직 성공으로 판정하지 않음.
- 터치, 모델 가독성, 실제 단말 성능: 미검증.

브랜치: feature/v04-visuals-navigation. v0.3 브랜치를 바탕으로 작성. v0.3 PR이 아직 main에 병합되지 않았으므로 PR 비교 기준은 feature/placement-decks-projectiles이다. 자동 병합하지 않는다.
